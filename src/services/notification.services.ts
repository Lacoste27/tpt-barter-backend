export const addNotification = async (userId: number, message: string): Promise<any> => {
    const newNotification ={}
    return newNotification;
  };
  
  export const getUserNotifications = async (userId: number): Promise<any[]> => {
    return [];
  };
  
  export const markNotificationAsRead = async (id: string): Promise<any | null> => {
    return {};
  };
  
  export const getNotificationById = async (id: string): Promise<any | null> => {
    return {};
  };
  
  export const getNotifications = async (): Promise<any[]> => {
    return [];
  };